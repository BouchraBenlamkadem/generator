import React from 'react';
import { View } from 'react-native';

const {{properCase name}}Template = ({ {{#each props}}{{this.name}}, {{/each~}} }) => <View style=\{{ flex: 1, backgroundColor: "black" }} />;

export default {{properCase name}}Template;
